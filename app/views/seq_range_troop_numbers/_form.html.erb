<%= form_for(seq_range_troop_number) do |f| %>
  <% if seq_range_troop_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seq_range_troop_number.errors.count, "error") %> prohibited this seq_range_troop_number from being saved:</h2>

      <ul>
      <% seq_range_troop_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rtn %>
    <%= f.number_field :rtn %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
